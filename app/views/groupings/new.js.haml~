/* Remove the join button */
/*$("#group").hide() */
/* Reload the group they joined */
$("#group_#{@group.id}").html("#{escape_javascript(render(:partial=>'group'))}");
/* add something to show they joined the group */
$('#groupdiv').after("You have left this group")

